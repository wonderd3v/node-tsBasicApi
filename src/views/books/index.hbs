{{#if books}}
{{#each books}}
<div class="col-md-4">
    <div class="card card-body">
        <h3>{{tittle}}</h3>
        <h5>Author: {{author}}</h5>
        <h6>Isbn: {{isbn}} </h6>
    </div>
</div>

{{/each}}
{{/if}}